# packages/ — placeholder for v2

Reserved for a future offline/cached `.deb` package store, for environments
without direct internet access during the first-boot pipeline. See
`docs/ROADMAP.md`. Empty in v1 — every stage script installs packages from
their respective live apt repositories.
