# configs/ — placeholder for v2

Reserved for host configuration files not yet part of v1 (e.g. `sshd_config`
hardening, `ufw` rules, `sysctl.conf` tuning, `fail2ban` jails). See
`docs/ROADMAP.md`. Empty in v1 — no files here are fetched or referenced by
any current pipeline stage.
