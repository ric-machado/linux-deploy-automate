# templates/ — placeholder for v2

Reserved for per-host templated configuration files (e.g. rendered with
host-specific variables at first-boot time). See `docs/ROADMAP.md`. Empty in
v1 — no templating mechanism exists in the current pipeline.
